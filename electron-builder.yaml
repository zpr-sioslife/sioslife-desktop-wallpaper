productName: SiosDeskPaper
asar: false
files:
  - build/**/*
  - .env
publish: 
  provider: github
  releaseType: draft
win:
  target: 
    - target: nsis
      arch: x64
  artifactName: ${productName}.${ext}
mac:
  type: development
  target: 
    - target: default
  artifactName: ${productName}.${ext}