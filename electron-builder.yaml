productName: SiosDeskPaper
asar: true
files:
  - build/**/*
publish:
  provider: github
  releaseType: draft
win:
  target:
    - target: nsis
      arch: x64
  artifactName: ${productName}.${ext}
mac:
  type: development
  target:
    - target: default
  artifactName: ${productName}.${ext}
